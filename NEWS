Package: mdi
============

Version: 0.0.0-9002 [2022-03-23]

SIGNIFICANT CHANGES:

 * Now 'mdi build' and 'mdi inject' uses .mdi/ for code blocks.

 * Now the code-block prefix no longer includes the file extension of
   the source file.  For example, 'mdi build r.sh' will now use prefix 'r'
   whereas in the past it was 'r.sh'.
 

Version: 0.0.0-9000 [2020-08-27]

NEW FEATURES:

 * Added mdi() and mdi_inject().

 * Package created.
